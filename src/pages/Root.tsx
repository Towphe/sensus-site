import { Outlet } from "react-router-dom";

function Root(){
    return (
        <>
            {/* re-setup styles here later */}
            <Outlet />
            
        </>
    )
}

export default Root;